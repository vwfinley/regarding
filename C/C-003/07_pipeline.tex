\section{Automation Pipeline}
TODO Write me!
- GitHub actions
- yaml file
- GitHub runner
- Link to code




\subsection{From inside devcontainers}
- VSCode
- .devcontainer/devcontainer.json
- Dockerfile
- Additional packages

\begin{mdframed}[backgroundcolor=black!20,leftmargin=0.0cm,skipabove=0.4cm,hidealllines=true,
  innerleftmargin=0.1cm,innerrightmargin=0.1cm,innertopmargin=0.15cm,innerbottommargin=-1.4cm]
\begin{code}[caption={\\TODO}, label=lst:lst3]
#!/bin/bash

# Run this inside developer container.
INFILE=main
OUTDIR=output
OUTFILE=C-003

mkdir $OUTDIR

latex -output-format=pdf \
  -output-directory=$OUTDIR \
  $INFILE.tex

pushd $OUTDIR
mv $INFILE.pdf $OUTFILE.pdf
popd
\end{code}
\end{mdframed}


\subsection{From host (outside guest container)}

\begin{mdframed}[backgroundcolor=black!20,leftmargin=0.0cm,skipabove=0.4cm,hidealllines=true,
  innerleftmargin=0.1cm,innerrightmargin=0.1cm,innertopmargin=0.15cm,innerbottommargin=-1.4cm]
\begin{code}[caption={\\TODO}, label=lst:lst4]
#!/bin/bash

# Run this from host.
INFILE=main
OUTDIR=output
OUTFILE=C-003

mkdir $OUTDIR

docker run --rm -it -v $(pwd):/workspace \
  ghcr.io/vwfinley/texlive-dev:latest \
  latex -output-format=pdf \
  -output-directory=$OUTDIR $INFILE.tex
  
pushd $OUTDIR
mv $INFILE.pdf $OUTFILE.pdf
popd
\end{code}
\end{mdframed}

Refer to Listing \ref{lst:lst4}.



\subsection{Makefile}
Other possibilites: Makefile, gotask task runner https://taskfile.dev/

\begin{verbatim}


\end{verbatim}


