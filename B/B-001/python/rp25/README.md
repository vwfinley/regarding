To get help run: `./rp25.sh -h`

Calculates critical points for the RP25 contour.
To calculate a contour for a specific wheel code and tread taper slope: `./rp25.sh -c <code> -s <slope>

For example: `./rp25.sh -c 110 -s 3.0`